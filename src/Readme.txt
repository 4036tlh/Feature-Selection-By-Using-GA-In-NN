*This program involves the python code
 	=please ensure you have install python with add path to the operating system

*For running this c++ code, you need to 
	=have a folder called "data" in the same dictionary with this txt
	=have a text file called "history" inside the folder "data"

*Now you can run the code in any c++ compiler


### IMPORTANT, ITS EXTRA NOTE ###
im using c++ to compile, but because i need to test each set of selected attributes, i use python as backend too